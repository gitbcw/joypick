## 使用信息
- 用户ID：`1904646`
- AppKey：`bd47e4bc-668a-4fc9-b2cb-18c53398782a`
- 快递公司编码：`ZTO`
- 运单号：`78963716282576`
- 请求类型：`1002`（即时查询）
- 正式端点：`https://api.kdniao.com/Ebusiness/EbusinessOrderHandle.aspx`

## 验证方式
- 方式A（直接调用快递鸟）：
  - 构造 `RequestData` JSON：`{"OrderCode":"","ShipperCode":"ZTO","LogisticCode":"78963716282576"}`
  - 计算签名 `DataSign`（与系统一致）：
    - 用 UTF-8 得到 `content = RequestData原文 + AppKey`
    - 生成 MD5 十六进制小写字符串
    - 将该字符串按 UTF-8 转字节后 Base64 编码
    - 最后对 `RequestData` 与 `DataSign`做 URL(utf-8) 编码
  - POST 表单参数：`RequestData`、`EBusinessID`、`RequestType=1002`、`DataSign`、`DataType=2`
  - 解析返回：输出 `Success/Reason/State/Traces`，给出结论
- 方式B（调用系统只读端点，如后端已运行）：
  - 管理端查询：`GET /admin/order/express?expCode=ZTO&expNo=78963716282576`
  - 返回 `expressInfo` 与 `Traces`（来源：litemall-admin-api/src/main/java/org/linlinjava/litemall/admin/web/AdminOrderController.java:82）

## 预期结果与判定
- 成功：返回包含 `Traces` 列表（时间 `AcceptTime`、节点 `AcceptStation`），`Success=true`
- 套餐未开通或额度不足：返回 `Success=false`，`Reason` 包含“没有可用套餐”或类似描述
- 编码/单号不匹配：返回 `Success=false`，`Reason` 包含“编码与运单号不匹配”

## 交付内容
- 原始响应内容（脱敏处理关键参数）
- 结构化结论：成功/失败原因、是否需要开通或调整
- 如需：给出沙箱端点验证与下一步建议（例如开通套餐后复测）

## 备注
- 本次仅做一次查询验证，不改代码、不提交密钥；日志中不回显完整密钥。
- 系统现有实现已与 `1002` 对齐（参考：litemall-core/src/main/java/org/linlinjava/litemall/core/express/ExpressService.java:86-102；litemall-wx-api/src/main/java/org/linlinjava/litemall/wx/service/WxOrderService.java:172-234），验证通过后小程序即可展示轨迹。